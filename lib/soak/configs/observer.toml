startup_delay_seconds = 0
prometheus = "http://localhost:9090"
experiment_name = "foobar"
vector_id = "featuresha-commitsha"
query = "sum(rate((bytes_written[1m])))"
capture_path = "/tmp/captures.txt"